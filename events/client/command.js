module.exports = (client, [message, command]) => {
  /**
   * Hi! It's a raw command event. 
   * Here we can handle when command is used. (It's a custom event)
   */
  //  console.log(message.author.tag, command)
};