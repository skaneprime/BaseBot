let BaseCommand = require('../../classes/BaseCommand');
module.exports = class extends BaseCommand {
    constructor() {
        super({
            name: "Command name",
            aliases: ["aliases"],
            category: "category name",
            usage: "usage",
            description: "description",
            guildOnly: true, // только на сервере, ЛС не используется!
            allowed_guilds: [/*СЕРВЕРА НА КОТОРЫЙ РАБОТАЕТ ЭТА КОМАНДА ОСТАВЛЯЙТЕ [] - для отключения*/],
            cooldown: 0 // кулдаун
        });

        this.execute = (client, message, args, ...params) => {
            //код
        };
    };
};